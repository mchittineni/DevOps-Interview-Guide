# Documentation site dependencies. Pinned so a docs build is reproducible and
# a Material release cannot change the site without a commit.
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-git-revision-date-localized-plugin==1.3.0